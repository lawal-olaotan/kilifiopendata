import Item from './Item';

var Items = [
    new Item(
        "#8CBE40",
        (type) => type === "subCounty"

        ),
    new Item(
        "#E4A628",
        (type) => type === "ward"
        )
]

export default Items